name: Central production deployment

on:
  workflow_dispatch:
    inputs:
      target:
        description: Deployment target
        required: true
        default: all
        type: choice
        options: [all, maple, restok]
      force:
        description: Force redeploy
        required: false
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: production-deployment-orchestrator
  cancel-in-progress: false

jobs:
  deploy-maple:
    if: ${{ inputs.target == 'all' || inputs.target == 'maple' }}
    uses: ./.github/workflows/_deploy-service.yml
    with:
      service: maple
      force: ${{ inputs.force }}
    secrets: inherit

  deploy-restok:
    if: ${{ inputs.target == 'all' || inputs.target == 'restok' }}
    uses: ./.github/workflows/_deploy-service.yml
    with:
      service: restok
      force: ${{ inputs.force }}
    secrets: inherit
